I want you to build a complete full-stack web application based on the following research project:

Project Title:
Secure Inter-Hospital Communication System Using DNA Encryption and LSB Image Steganography

🔐 Core Features to Build:
1. Web Interface

Build a clean, modern web UI using React + TailwindCSS (or any stack you prefer).
Pages required:

Home

Encryption & Embedding

Extract & Decrypt

Live Transfer (Sender ↔ Receiver)

DICOM Viewer

Chatbot Page

2. DNA Encryption Module (Backend in Python or Node.js)

Implement the following pipeline:

Binary → DNA Encoding

00 → A

01 → T

10 → C

11 → G

Symmetric DNA Substitution Cipher:

A ↔ T

C ↔ G

(Keep this reversible so Receiver can decode.)

Optional: Add AES-256 layer on final DNA string.

Expose this as API endpoints:

POST /encode-dna
POST /decode-dna

3. LSB Steganography Module

Implement LSB embedding for images:

Embedding Steps:

Take DNA ciphertext → convert back to binary

Replace least significant bit of each pixel with encrypted data

Add end-marker pattern (like 000111000111)

Extraction Steps:

Scan LSB bitstream

Stop at the end marker

Convert binary → DNA → text

API endpoints:

POST /embed
POST /extract

4. Real-Time Transmission Using Socket.IO

Create a Sender Clinic and Receiver Clinic channel.

Features:

Live transmission of stego-images

Status notifications

Auto reconnect

Sub-second latency

Build Socket.IO server + frontend listeners.

5. DICOM Viewer

Add DICOM medical image viewer:

Display slices (CT/MRI)

Use dicom-parser + cornerstone.js (frontend)

Allow drag-and-drop upload

6. Analytics Panel

Integrate Matplotlib-style graphs (or JS charts) to show:

Processing time (encoding/decoding)

PSNR / SSIM values

Payload length

Transmission latency

Backend can compute PSNR + SSIM using Python (skimage).

7. Chatbot Integration

Add a simple chatbot using any LLM API.
Features:

Explain encryption

Help user navigate the platform

Guide doctors/admins

8. System Requirements

Make sure the project includes:

Full folder structure

Backend + frontend integration

Working APIs

Real-time event handlers

Ability to run locally & deploy

Instructions in README

9. Deliverables Needed

Please generate:

Complete full-stack code

Backend API code (all endpoints)

Frontend UI + components

Socket.IO sender/receiver implementation

DICOM viewer code

Chatbot integration

Utility functions (DNA encryption, LSB embed/extract)

README with setup instructions

Make everything modular, clean, well-documented.

10. Development Constraints

Use any stack you prefer, but here are recommended:

Frontend:

React + TailwindCSS

Backend:

Python (FastAPI) or Node.js (Express)

Socket.IO

Pydicom for DICOM

Pillow/OpenCV for image processing

Your task:

👉 Generate the full working web app codebase
👉 Explain all files
👉 Provide installation + run instructions
👉 Ensure the app fully implements the research-paper system